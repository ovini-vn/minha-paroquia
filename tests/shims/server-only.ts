// Stub para testes: o pacote real "server-only" só existe para lançar erro
// quando importado por um bundle de cliente (webpack). Em Vitest (Node puro),
// substituímos por um módulo vazio via alias em vitest.config.ts.
export {};
